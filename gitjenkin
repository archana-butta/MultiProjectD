pipeline {
    agent {label slave job}
    stages {
        stage('Build') { 
            steps {
              echo "hello archana" 
                // 
            }
        }
        stage('Test') { 
            steps {
              echo hello 
                // 
            }
        }
        stage('Deploy') { 
            steps {
              echo hii
                // 
            }
        }
    }
}
