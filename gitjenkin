pipeline {
    agent {label slave}
    stages {
        stage('Build') { 
            steps {
              echo "hello archana" 
                // 
            }
        }
        stage('Test') { 
            steps {
              echo hello 
                // 
            }
        }
        stage('Deploy') { 
            steps {
              echo hii
                // 
            }
        }
    }
}
